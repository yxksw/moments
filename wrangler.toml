# EdgeOne Pages 配置文件
# 用于配置 Functions 和环境变量

name = "moments-blog"
compatibility_date = "2024-01-01"

# 环境变量（在 EdgeOne 控制台设置）
[env.production]
vars = { ENVIRONMENT = "production" }

# 密钥（在 EdgeOne 控制台设置，不要提交到 Git）
# NOTION_TOKEN = "your_notion_token"
# NOTION_DATABASE_ID = "your_database_id"
